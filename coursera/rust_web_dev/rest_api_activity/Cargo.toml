[package]
name = "rest_api_activity"
version = "0.1.0"
edition = "2024"

[dependencies]
rocket = "0.5.0-rc.1"  # Use the latest stable version available
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"